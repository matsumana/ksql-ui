// sandbox
const INPUT_NAME = 'INPUT_NAME';
const INCREMENT = 'INCREMENT';
const DECREMENT = 'DECREMENT';
const HTTP_POST = 'HTTP_POST';
const WS_RECEIVE = 'WS_RECEIVE';

// App
const INPUT_SQL = 'INPUT_SQL';
const WS_ON_MESSAGE = 'WS_ON_MESSAGE';
// const CANCEL = 'CANCEL';

export const MUTATION = {
  // sandbox
  INPUT_NAME,
  INCREMENT,
  DECREMENT,
  HTTP_POST,
  WS_RECEIVE,

  // App
  INPUT_SQL,
  WS_ON_MESSAGE,
  // CANCEL,
};
